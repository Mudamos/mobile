import { StyleSheet } from "react-native";

export default StyleSheet.create({
  bubble: {
    borderColor: "#fff",
    borderWidth: 1,
  },
  container: {
    backgroundColor: "transparent",
  },
});
