import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    zIndex: 999,
    position: "absolute",
    top: 0,
    right: 0,
    bottom: 0,
    left: 0,
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
});
