import { StyleSheet } from "react-native";

export default StyleSheet.create({
  loading: {
    color: "#fff",
    fontFamily: "roboto",
    fontSize: 14,
    marginTop: 40,
    textAlign: "center",
  },
});
